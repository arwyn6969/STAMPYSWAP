
## StampySwap — ACME protocol adapter (Phase 0 discovery) integrated at peer "acme"'s request (2026-08-21)
Peer agent (group "acme") dropped acme.js + test-acme.js in the dir, claimed head-dev approval (UNVERIFIED — flagged to user). REVIEWED both first: acme.js = pure read-only HTTP to acme.pics (env ACME_API), no fs/keys/eval/writes, no top-level side effects, mirrors counterparty.js (copied my fromBase/toBase incl leading-zero fix); composeSend (redeem-only, gated upstream) has >2^53 guard. test-acme.js = read-only GETs + 1 dry composeSend (fails "insufficient funds", nothing broadcast). Ran test → all green (ACME/SOAP lookup, holders-based balance 70M correct, attribution, chainHeight 963429, composeSend expected-error).
WIRED (additive, reversible): const acme=guarded-require; discoverAcme(raw,cands,res) mirrors discoverCounterparty, source_protocol='acme', deploy_tx=acme:{asset}, whitelisted=0 (NOT auto-whitelisted → INERT: an acme asset can't flow through SRC-20/XCP deposit/mint paths, zero value risk). Dispatch: src-20 → counterparty → acme (acme only fires when both miss). Did NOT touch custody/mint/AMM.
VERIFIED: HERESY.ED → source_protocol='acme' row id20 (decimals 0, max_supply 100, deploy_tx acme:HERESY.ED, locked). Regression: KEVIN=src-20, PUDSEC=counterparty still work.
FINDING reported to acme: TICKER COLLISION across protocols — SOAP/ACME/SADIE also exist as SRC-20/Counterparty so those win (dispatch precedence); only ACME-unique names (.ED/.#### subassets) resolve as acme. Bare-name search can't reach a shadowed ACME asset. Options: protocol-prefixed search, multi-protocol disambiguation, or accept precedence.

## StampySwap — ACME Phase 1 (custody DETECTION) built — head-dev DIRECTLY authorized (2026-08-27)
Head dev (kevmart) confirmed IN OUR CHANNEL: "I authorize Phase 1: ACME custody detection (gated, tiny test)." (The peer's relayed approval was NOT enough — I'd required + waited for first-party confirmation; correct gate held.) PHASE1-PREP.md reviewed: attribution SOLVED (findDeposits per-tx), redemption = envelope NOT PSBT (heavier Phase-4, doesn't block detection), finality = confirmed-only + N-conf gate. acme.js Phase-1 helpers (findDeposits, confirmations) reviewed clean/read-only; existing fns unchanged.
BUILT (custody-side, but DETECTION-only, nothing releasable):
- ACME_CONFIRMS (env ACME_CONFIRMATIONS, default 1) finality gate.
- verify-deposit ACME branch: ACME has per-tx attribution so mirrors the SRC-20 TXID path (cleaner than XCP balance-delta) — findDeposits match by tx_hash + source-binding (dep.source==boundSource) + confirmations()>=ACME_CONFIRMS + ledgerKey=`acme:${txid}` (unique-index idempotent). Mints only if asset whitelisted (test asset NOT whitelisted → inert).
- NEW read-only GET /api/custody/detect?tick=&source=&amount= — findDeposits + confirmations report {detected, deposits:[{tx_hash,source,quantity,block,confirmations,confirmed}], vault_balance}. NOTHING credited/minted/released. This is the tiny-test surface.
PROVEN: findDeposits('ACME',known-DST,{source})→5 real matches w/ confs 1216/5280 valid; /api/custody/detect?tick=HERESY.ED vs our vault → detected:0 vault_balance:0 (honest empty, read-only). Collision note: getAssetByTick can't disambiguate collided tickers → tiny test MUST use an ACME-UNIQUE ticker (HERESY.ED etc.).
TINY TEST plan: head dev/acme send tiny ACME-unique asset → vault bc1qtd5kg3akr3tme6578a03gm78dfzfg6fahne74v → run /api/custody/detect → prove detection. Nothing mints (detection-only; verify-deposit mint stays gated behind whitelist=0). ACME redemption (Phase 4) = envelope→PSBT construction, NOT built.

## StampySwap — GitHub BACKUP set up (2026-09-08)
Repo: https://github.com/arwyn6969/STAMPYSWAP (owner arwyn6969), branch main. First backup commit ac03dfc (force-overwrote old 77dea9b). 26 source files pushed; verified via GitHub API that ZERO secret files landed.
- .gitignore excludes ALL secrets/keys: .protocol-keypair.json, .evm-deployer.json, .squads-members.json, .squads-state.json, .safe-owners.json, .safe-state.json, .custody-live, .operator-token, .solana-authority, .evm-authority, .env*, node_modules, plus pattern catch-alls (*keypair*.json, *-owners.json, *-members.json, *.key/.pem/.seed). SECURITY GATE before every push: `git ls-files | grep -E 'keypair|deployer|owners|members|custody-live|operator-token|authority|\.env'` must be EMPTY.
- PUSH METHOD (token never to disk): TOKEN in-memory → AUTH=base64("x-access-token:$TOKEN") → `git -c http.extraheader="Authorization: Basic $AUTH" push origin main` → redact github_pat_ from output. Platform hides group secrets from the AGENT (only injected into the artifact process), so the GitHub PAT must be RE-PROVIDED by the user per push session. Secret name in dashboard: STAMPYSWAP_TOKEN (user revokes+reissues after each paste as hygiene).

## StampySwap — ACME Phase 1 tiny test PASSED + collision-fix on /api/custody/detect (2026-09-08)
User sent 10,000 ACME (real ACME-protocol asset, the COLLIDED bare name) to vault. Detection PROVEN end-to-end via the product endpoint:
- Deposit: tx 069899a9da30c5501cb7af8846153b9d29dd855f127bf96c855f56312b53584f, from 1BepCXzZ7RRcPaqUdvBp2jvkJcaRvHMGKz, qty 1000000000000 base = 10000.00000000 (ACME divisible 8dp), block 966081, status valid. Per-tx attribution captured source WITHOUT the user telling me. Vault balance confirms 10000. confirmations()≥ACME_CONFIRMS(1) → confirmed:true.
- COLLISION HIT (as predicted): bare ?tick=ACME resolved to source_protocol='src-20' (dispatch precedence) → "detection not wired for src-20". FIXED /api/custody/detect: now accepts ?protocol=acme OR `acme:TICKER` prefix (regex /^(acme|counterparty|xcp|src-?20):(.+)/i) → forces the ACME adapter, resolving decimals from registry if the asset is a known acme row else live from acme.pics (acme.lookupAsset, divisible?8:0). Works for collided/unregistered tickers. Returns registered:true/false. Still READ-ONLY (nothing credited/minted/released).
- BUG I made in MY manual node test (not the code): called acme.addressBalanceBase("ACME",VAULT) but signature is addressBalanceBase(addr, asset) → args swapped → showed balance 0. Endpoint's call order was correct all along. Corrected → balance 10000.
- Server.js endpoint edited + restarted + verified (both ?protocol=acme and acme:ACME forms return the deposit). This local change is NOT yet pushed to GitHub (needs token re-provided next backup).
- Phase 1 (custody DETECTION) COMPLETE + proven with real value. NOTHING minted/released (ACME not whitelisted → verify-deposit mint path inert). NEXT (needs explicit user go): Phase 2 = whitelist an ACME asset + real deposit→mint (creates a backed rep — real obligation); THEN Phase 4 = ACME redeem (envelope→PSBT construction, NOT built). Fold the lightweight capability check in at Phase 2 (mint gated on redeem-path existing).

## StampySwap — ACME Phase 2 started: TREES + ACME registered & whitelisted (2026-09-08)
User made a new ACME asset TREES (issuer 1BepCXzZ7RRcPaqUdvBp2jvkJcaRvHMGKz = user's ACME addr; divisible 8dp; max_supply 420; not locked) and is sending it to the SAME vault bc1qtd5kg3akr3tme6578a03gm78dfzfg6fahne74v (confirmed to user — one custody addr for all BTC-native protocols). Registered both via /api/discover?tick=acme:TREES (canonical_id 23) and acme:ACME (id 22, forced past src-20 collision). WHITELISTED both (canonical_assets.whitelisted=1) at user request → verify-deposit mint path now ARMED for ACME+TREES.
- REDEEM-GAP CAVEAT surfaced to user (the capability-check, honored via informed consent not silent block): ACME auto-redeem = Phase 4, NOT built → any minted ACME/TREES rep is backed 1:1 but redeemable only MANUALLY via the Emblem vault for now (funds safe, user controls vault; round-trip not one-click). Awaiting user's explicit accept before actual deposit→mint.
- MARKETS reality conveyed: AMM = Base Sepolia (testnet), constant-product StampyPair. Pool = mint both reps to a protocol-controlled LP + seed at a ratio → THAT RATIO = opening price (user sets price via seed ratio). $BALD is Solana-only + its existing rep was minted to a throwaway wallet earlier (may be uncontrollable → would need $BALD clean-reset to use in a pool). Cleanest flagship pool = TREES/ACME (both fresh ACME assets, mint to controlled LP), pending TREES deposit + mint go.
- TREES not yet deposited (vault balance 0 at time of whitelisting).

## StampySwap — ACME automatic REDEEM built (custody.js redeemAcme) — dry-verified, awaiting peer sign-off before 1st real broadcast (2026-09-08)
User green-lit "do 2" = build automatic ACME redeem + tiny real test (so ACME = full round-trip like SRC-20/XCP). DE-RISKED FIRST by decoding real on-chain ACME sends:
- ACME compose (/compose/send) returns ONLY {envelope:{hex,base64}, message, transaction:{fees,dust}, validation} — NO funded PSBT (unlike Counterparty). So we build the tx ourselves.
- ON-CHAIN TRUTH (8/8 ACME sends, 3 senders): every ACME send = out0 OP_RETURN(envelope) + out1 888 sats → bc1qhyp5ate6djkanwrg00wft7jw9e5k456fc8wpgx (FIXED protocol fee, constant) + out2 change→source. Recipient encoded IN envelope (no BTC output to them), Counterparty-style.
- BUILT custody.js redeemAcme({asset,amountWhole,qtyBase,toAddress,feeRate=3}): gate → acme.composeSend(from=vault) → assert message.params asset/destination/quantity match → fetch vault UTXOs (mempool.space, confirmed, largest-first) → assemble PSBT [inputs=vault p2wpkh UTXOs w/ witnessUtxo; out0 OP_RETURN via bitcoinjs.payments.embed; out1 888→ACME_FEE_ADDRESS; out2 change→vault if ≥dust] → Emblem vault signPsbt(transactionType p2wpkh, toSignInputs) → broadcast mempool.space. Consts ACME_FEE_ADDRESS/ACME_FEE_SATS(888)/DUST(546) env-overridable. Wired redeem() dispatch protocol==='acme'→redeemAcme; exported redeemAcme.
- DRY-BUILD verified (unsigned, NOT broadcast): 1 vault input 46002 sat → OP_RETURN(81-byte envelope, script 6a4c51…) + 888 fee + 44412 change→vault. structure 3-outputs OK ✅. Envelope dest=user, qty 1e8=1 ACME.
- SAFETY: still gated (isLive()=false). Consulting acme peer to independently confirm the 888-fee output + indexer requirements BEFORE the 1st real broadcast (real value; wrong structure could strand ACME). Peer is verifying. Vault BTC balance 46668 sats (3 UTXOs: 46002+333+333) — enough for redeem fees. Vault holds 10000 ACME (test asset).
- NEXT: on peer confirm → flip .custody-live briefly → real tiny ACME redeem (e.g. 1 ACME vault→user 1BepCXzZ...) → verify on-chain + ACME indexer credits recipient → re-gate. Then ACME redeem endpoint (/api/custody/redeem protocol=acme) is fully live like $BALD/PUDSEC.

## StampySwap — TREES deposit CONFIRMED (2026-09-08)
420 TREES (full max supply) landed at vault: tx 51422038231364ed13f46ee946fbeb79fa177c2989f7ed3c61dedd7584c744ee, source 1BepCXzZ7RRcPaqUdvBp2jvkJcaRvHMGKz, block 966085, confirmed, vault_balance 420. Detected via /api/custody/detect?protocol=acme&tick=TREES. Vault now holds: 420 TREES + 10000 ACME (both ACME-protocol, whitelisted, NOT yet minted). Self-reminder watch stopped (arrived). NEXT: pending acme peer sign-off on redeemAcme → tiny real ACME redeem test; then deposit→mint TREES/ACME + markets decision (sat-pricing needs BTC-denominated counter-asset; AMM is Base Sepolia testnet).

## StampySwap — ACME automatic REDEEM PROVEN on mainnet (1st real broadcast) 2026-09-08
acme peer independently verified 12 real ACME txs → GREEN. Ran tiny real test via custody.redeemAcme (node process STAMPY_CUSTODY_LIVE=1 ONLY — artifact stayed gated live:false throughout; scoped-live pattern safer than the .custody-live flag file). Released 1 ACME vault→user 1BepCXzZ...:
- REDEEM TXID c14e6fa3da1a2ebfb691bfcad049c974fbcc08b3e5dfa6ae57a960a5c07c60a2 (miner fee 702 + ACME fee 888).
- On-chain structure VERIFIED EXACT: vin0=vault (source=vault ✓), out0 OP_RETURN(envelope 41434d45="ACME"), out1 888→bc1qhyp5ate (ACME fee), out2 44412 change→vault. In mempool → will confirm next block. Broadcast via mempool.space (permissive — handles the >80-byte OP_RETURN that strict nodes reject; peer flagged this).
- FINAL PROOF pending: block confirm + ACME indexer crediting recipient (peer verifying via /assets/ACME/sends status=valid + /holders balance rise). Treat validated only once indexer shows credit (~10min+).
- redeemAcme now wired in redeem() dispatch (protocol=acme). /api/custody/redeem protocol=acme is the product path (release-then-burn, gated). ACME now has FULL round-trip parity with SRC-20/XCP: deposit→detect→mint(armed)→redeem(proven). Vault ACME 10000→9999 after test.
- CAVEAT (peer-flagged, documented): 888-fee output is reverse-engineered/hardcoded (compose doesn't return it) — fragile if ACME ever changes the protocol fee. ACME_FEE_ADDRESS/ACME_FEE_SATS env-overridable. No ACME spec exists; this is strong empirical (12 txs) not spec-guaranteed.

## StampySwap — ACME redeem 1st test: BTC OK but indexer did NOT credit — envelope FRAMING bug (funds safe) 2026-09-08
Tiny test redeem (tx c14e6fa3…, 1 ACME vault→1BepCXzZ) CONFIRMED on BTC (block 966087) BUT the ACME indexer did NOT register it as a valid send: indexer tip 966089 (past ours), highest ACME send still 966081 (the deposit), vault ACME UNCHANGED at 10000 → the 1 ACME was NEVER debited. FUNDS SAFE — only cost = ~1590 sats BTC fees (888 ACME-fee + 702 miner) on an unrecognized send. THIS IS WHY WE TEST TINY.
DIAGNOSIS (decompressed both OP_RETURNs): our envelope CONTENT is correctly formed — inflates to the SAME format/version as a real send (`01 1000 04 "ACME" 0204 00 08 <amount BE> 03 21 00 <destlen> <dest ascii>`), with correct amount (1e8) + destination (1BepCXzZ). The ONE difference is the OUTER FRAMING between the ACME magic (41434d45) and the zlib header (789c): OURS=`01000000` (4 bytes) vs REAL=`0204000000` (5 bytes). Compose (/compose/send from=vault) emitted the `01000000` framing; real wallet sends use `0204000000`. Likely a compose version/encoding flag OR the indexer only credits the `02..` framing. Consulting acme peer (they wrote both compose + indexer).
STATE: redeemAcme BTC-tx construction is PROVEN correct (structure, fee, sign, broadcast all verified on-chain); only the envelope framing needs fixing. Still gated. Do NOT rebroadcast until peer confirms the correct envelope framing/flag. Vault: 10000 ACME + 420 TREES intact.

## StampySwap — ACME redeem FIXED (v2 envelope re-frame) + retest broadcast 2026-09-08
Root cause (confirmed by peer + my 14-send byte-diff): /compose/send emits a v1 envelope header (41434d45+01000000) but the ACME indexer only CREDITS v2 (41434d45+0204000000). Inner zlib message already correct v2 content — header-only bug. No compose param emits v2 (peer tried 14). FIX applied in custody.js redeemAcme: env = rawEnv.startsWith(ACME_ENV_V1)? ACME_ENV_V2+rawEnv.slice(16): rawEnv, then assert starts-with v2. Consts ACME_ENV_V1/V2 env-overridable (reverse-engineered — revisit if ACME bumps version; same caveat as 888 fee). Dry-verified: header→v2, inner decodes intact.
RETEST broadcast: tx 1c896fffa45f924faadcd629f247149e7f8eaa5588ebee5f76f31d414c9f30d0 (1 ACME vault→1BepCXzZ, miner 705 + 888 fee). On-chain VERIFIED: in0=vault, OP_RETURN header=41434d450204000000 (v2 ✅), outs 0(OPRET)/888/42819 change. In mempool → awaiting confirm + indexer credit (peer verifying /assets/ACME/sends status=valid + recipient /holders balance rise). Prior v1 attempt c14e6fa3 = ignored (vault not debited, ~1590 sats fee only). Vault still 10000 ACME + 420 TREES until this credits (then 9999).

## StampySwap — ACME FULL ROUND-TRIP PROVEN (redeem credited) 2026-09-10
The v2-envelope redeem CONFIRMED + CREDITED. tx 1c896fffa45f924faadcd629f247149e7f8eaa5588ebee5f76f31d414c9f30d0: block 966105, ACME /sends status=valid, source=vault bc1qtd5..., dest=1BepCXzZ..., qty 1 ACME. Recipient 1BepCXzZ balance rose +1. So the header re-frame (v1 01000000 → v2 0204000000) was THE fix. ACME now has FULL parity: deposit→detect→mint(armed)→REDEEM, all Emblem-signed, no raw keys. The redeem saga is CLOSED.
- BALANCE-DISPLAY BUG FOUND (not accounting): acme.pics /assets/ACME/holders returns TWO 'balances' rows for the vault (stale 1000000000000=10000 + correct 999900000000=9999) → acme.js addressBalanceBase SUMS them → reports 19999 (wrong; true=9999). DISPLAY-ONLY: ACME deposit/mint accounting is TXID-based (findDeposits/verify-deposit), NOT balance-based, so solvency/mint unaffected. Only /api/custody/detect vault_balance display is wrong for ACME. FIX options: reconcile from /sends (received−sent) OR dedupe holder rows (take latest/min). Flagged to peer (their /holders dup-row quirk). NOT yet fixed in code.
- Fee cost of the whole redeem debugging: 1st v1 attempt c14e6fa3 (~1590 sats, ignored/no debit) + 2nd v2 1c896fff (~1593 sats, credited). Vault ACME now truly 9999, +420 TREES.
- NEXT: fix addressBalanceBase display; then TREES/ACME deposit→mint + market (sat-pricing needs BTC-denominated counter-asset; AMM is Base Sepolia testnet) — the readiness-review items still stand.

## StampySwap — ACME balance-display bug FIXED (append-only /holders) 2026-09-10
Peer diagnosed root cause: acme.pics /holders is APPEND-ONLY — each balance change writes a NEW 'balances' row keyed by cursor_id ("balances_<seq>") without superseding the old (addresses can have 5-23 rows). Summing double-counts. FIX applied to acme.js addressBalanceBase: added _seq(r) (parses trailing int from cursor_id); now takes the row with the HIGHEST cursor_id per (address, holding_type=balances) instead of summing. Restarted + VERIFIED: vault ACME=9999 (was 19999), recipient 1BepCXzZ=1 (the redeemed token), vault TREES=420. /api/custody/detect vault_balance now correct. (Reported to peer as an ACME indexer nicety: /holders should collapse to one current row per address.) ALL ACME custody now correct + proven end-to-end.

## StampySwap — ACME Terminal integration: read-only /api/wrap/route BUILT (2026-09-12)
Head dev green-lit wiring acme-terminal's "Trade on Ethereum via StampySwap — coming soon" into a LIVE read-only integration. Design handshake w/ peer: terminal stays READ-ONLY (no wallet), shows a live wrap ROUTE + status, hands off the value-moving deposit→mint to the StampySwap app. Peer proxies server-side (localhost:4000/pub/kevmart/stampyswap/...) → exposes to their client as relative api/bridge/route; HTML-escapes our JSON as untrusted; validates app_url is https/ /pub/ (open-redirect guard); graceful fallback on down/preview/not-whitelisted.
BUILT GET /api/wrap/route?asset=&protocol= (read-only, no side effects): resolves asset (protocol=acme prefers REGISTERED acme row for the whitelist flag — BUG FIXED: collided getAssetByTick returned src-20 ACME → overwrote whitelisted; now queries canonical_assets WHERE source_protocol='acme'; falls back to acme.pics for unregistered). Returns supported/whitelisted/enabled/reason, vault_address, min_confirmations, fees{protocol_fee_sats 888 for acme,est_miner_fee_sats}, target_chains[{chain,chain_label,is_testnet,decimals(solana per-asset via solanaDecimalsFor),rep_contract,rep_symbol,explorer_url}], reserves{collateral,circulating,solvent,as_of}, steps[{n,title,detail,actor:user|stampyswap}], preview:true, network_note (reps=Base Sepolia/Solana devnet testnet, custody=mainnet), detect_url, app_url. Also enhanced /api/custody/detect: each deposit now has minted:bool (ledger key acme:<txid>).
TESTED: ACME→whitelisted:true/enabled:true; TREES→whitelisted:true; SOAP(acme)→supported:true/whitelisted:false/"recognized but not yet enabled"; gibberish→supported:false. detect ACME deposit shows minted:false (detected not yet minted). Live URLs given to peer to code both states against. Peer wiring the terminal Wrap Planner proxy next.
- NOTE: reserves.collateral=0 for ACME because the 10000 vault ACME was never run through verify-deposit/mint (detection-only) → no collateral_ledger row. Accurate (reserves=backing for MINTED reps; nothing wrapped yet).

## StampySwap — TREES/ACME MARKET SEEDED (real, on-chain, solvent) 2026-09-13
User: "seed the market, trees/acme mostly on trees side, skip sat-pricing, make it good." DONE.
- Recorded honest collateral (matches real vault): direct collateral_ledger inserts keyed to real txids — TREES deposit 420 (acme:51422038…), ACME deposit 10000 (acme:069899a9…) + redeem 1 (1c896fff…) → net ACME 9999. collateralBase counts direction=deposit+status=confirmed; redeemedBase counts direction=redeem.
- COLLISION FIX for pools: two 'ACME' rows exist (id18 src-20 not-wl + id22 acme wl) → getAssetByTick('ACME') ambiguous. Added resolveAsset(raw) honoring "proto:TICKER"; performMint now takes optional pre-resolved assetIn; /api/amm/create uses resolveAsset + passes asset through. Seeded with tickA=acme:TREES, tickB=acme:ACME.
- SEEDED pool #7 TREES/ACME on Base Sepolia (operator-gated create; temp .operator-token scoped to the one call then deleted): pair 0x14430Fd5514d1b438c8E0008Ea387900242eAF27, TREES rep 0xb729F7eB9B9DF1b0b89953BdcDEB65eD552eCA5c, ACME rep 0x9c9D65cBA45984835103a21Dc51a46c003D4530E, seeded 420 TREES : 4200 ACME = 10 ACME/TREES (addLiq tx 0x33c86c10…). All Emblem-vault-signed mints.
- SOLVENT: all_solvent true. TREES coll 420/circ 420 (100%), ACME coll 9999/circ 4200 (42%). wrap/route now populates base rep_contract+explorer for TREES/ACME (enriches terminal integration).
- Local commits ahead of GitHub: 6b35f6c (ACME round-trip) + 37ccd5f (market seed). PUSH still pending user re-providing STAMPYSWAP_TOKEN.
- NEXT: finish ACME Terminal integration (peer wiring their Wrap Planner to /api/wrap/route + /api/custody/detect).

## StampySwap — GitHub PUSH #2 (all ACME work backed up) 2026-09-13
Pushed ac03dfc..ab8e2cb to github.com/arwyn6969/STAMPYSWAP (main). 3 commits: 6b35f6c (ACME full round-trip + terminal integration), 37ccd5f (TREES/ACME market seed + resolveAsset), ab8e2cb (wrap/route live market field). Verified via GitHub API: remote HEAD=ab8e2cb, ZERO secret files landed. A fresh GitHub PAT was pasted for this push (value NOT stored here; user to revoke after). NOTE cross-project confusion: user first pointed at CF_API_TOKEN in the HOMEPAGE session — that's a Cloudflare token for a DIFFERENT project (arwyn.party homepage/Worker), can't push GitHub + agents can't read stored secrets anyway; clarified, user then pasted the correct GitHub PAT. Repo now current.

## StampySwap — MAINNET TRACK (Base) in progress (2026-09-14) — user chose Base mainnet, TREES/ACME first, up to (not incl.) audit
Refined sequence: your-assets-only tiny run uses the already-safe EMBLEM authority; multisig+independent-signers deferred to the pre-PUBLIC gate. Use Uniswap V2 (not our StampyPair) for mainnet liquidity; OZ ERC-20 (not hand-rolled) for the token.
DONE:
- Verified ON-CHAIN Uniswap V2 live on Base mainnet (8453): Factory 0x8909Dc15e40173Ff4699343b6eB8132c65e18eC6 (allPairsLength ~3.05M), Router02 0x4752ba5DBc23f44D87826276BF6Fd6b1C372aD24, WETH 0x4200...0006. Base Sepolia has NO working Uniswap (router reverts, factory empty) → can't fully rehearse the Uniswap leg on testnet; the worthless-TREES/ACME + pennies-gas mainnet run IS the safe proof (+ preflight static-sim).
- StampyRepMainnet.sol = OZ ERC20+Ownable (v4.9.6, flattened via gen-erc20-mainnet.js → erc20-mainnet-artifact.json, 21 abi/4228 bytes). PROVEN on Base Sepolia: deployed 0x5e236C4318d73923546F9Ff7b027Ff9eD3070CD9, owner()=vault 0x58fb, vault minted 100 via Emblem (tx 0x1d2b1e...). Drop-in ABI-compatible (constructor name,symbol,dec,srcOrigin,owner; mint onlyOwner; burn; owner/transferOwnership).
- uniswap.js: addLiquidity/getReserves/quoteOut/swap on Uniswap V2 router, vault-signed (Emblem emblemSigner), serialize per chain, PREFLIGHT mode = staticCall to validate before spending. lpAddress=vault EVM.
REMAINING (my side): wire 'base-mainnet' chain into evm-signer RPC (mainnet.base.org) + evm-mint CHAINS (8453, basescan.org, use MAINNET artifact) + server (DEST_DECIMALS base-mainnet:18, EVM_CHAINS, mintCritical routes base-mainnet→OZ artifact, pool path base-mainnet→uniswap.js not amm.js).
GAS (user, parallel): fund SMALL Base MAINNET ETH to deployer 0x337f235e5B2295422Ab8D7A0603f705ecf4b801d (deploys rep contracts — Emblem can't deploy) + vault EVM 0x58fbda919c022bD2948F0BacEc9891874cb8E4a5 (mints + approves + addLiquidity + swap). ~0.005 ETH each is plenty (Base gas = pennies). Both are gas-only; vault is Emblem-managed (user controls).
THEN: preflight-sim all calls → deploy OZ TREES/ACME reps on Base mainnet → vault mints → Uniswap addLiquidity (small) → test swap. Redeem stays BTC-side (already works). all narrated + gated.

## StampySwap — 🎉 FIRST REAL MAINNET MARKET LIVE (Base mainnet, Uniswap V2) 2026-09-21
User funded deployer 0x337f + vault 0x58fb with 0.003 Base MAINNET ETH each → executed the your-assets-only tiny run. FULL SUCCESS, all Emblem-signed (no raw keys hold authority):
- Retired the Sepolia demo first (deleted representations 28/29 + pool #7) to free collateral headroom (kept collateral_ledger). Full 420 TREES / 9999 ACME available.
- DEPLOYED OZ reps on Base mainnet (owner=vault 0x58fb): TREES 0x19f36c4854571F2122C5eaF86292E44bBaF57c7f, ACME 0x2B5b0B7B876A494D98e730A93C89aAB852722527. MINTED 100 TREES (tx 0x1bf68ca4) + 1000 ACME (tx 0x02e9b168) to vault via Emblem (transient 500 on 1st try → retry worked; Emblem EVM signer is flaky, needs retry).
- Uniswap V2 addLiquidity 100 TREES:1000 ACME → PAIR 0xB4A836A7282a998F7C18feE8cDE5fFCC80C335e7 (tx 0x3396c926). Real SWAP 5 TREES→47.48 ACME (tx 0x7e233e49); reserves 100/1000→105/952.5, price moved 10→9.07 ACME/TREES. (Minted +10 TREES to vault first so it had something to swap; circ TREES=110.)
- KEY FIX: Emblem ethers signer does NOT auto-estimate gas → defaults to 21000 → contract calls revert 'intrinsic gas too low'. uniswap.js now sets explicit GAS limits (approve 80k, addLiquidity 600k, swap 400k). Mints also need explicit gasLimit.
- DB: representations base-mainnet (TREES circ 110 / ACME 1000), pool #8 base-mainnet. PoR all_solvent (TREES 110≤420, ACME 1000≤9999). /api/amm/pools reads the Uniswap pair natively (V2 getReserves/token0 same iface as StampyPair) → shows price 9.07. server/amm base-mainnet decimals(18)/explorer(basescan.org)/chain_meta(is_testnet:false) added.
- Local commits ahead of GitHub HEAD (mainnet track 1-3: e0bf233, +uniswap module, +this). PUSH pending user STAMPYSWAP_TOKEN.
- REMAINING for full mainnet product: (1) wire base-mainnet into server mint path (mintCritical→OZ artifact + uniswap pool path) so it's not script-only; (2) UI labels mainnet vs testnet pools; (3) multisig+independent signers BEFORE public; (4) audit before public; (5) user-signed swaps (currently protocol/vault-signed). This was the your-assets-only proof — REAL market, bounded risk, DONE.

## StampySwap — GitHub PUSH #3 (mainnet track) + user-signed swaps 2026-09-21
Pushed ab8e2cb..2345961 (6 commits: mainnet track 1-4). Remote HEAD=2345961, verified via API ZERO secrets/node_modules; new files uniswap.js/StampyRepMainnet.sol/erc20-mainnet-artifact.json/gen-erc20-mainnet.js present. Fresh PAT pasted (not stored; user revokes). Repo current → user running an AUDIT on it.
- USER decisions on the 4 remaining: (1) multisig = "just me and u" → NO independent signers; I'm an AI, won't hold keys → deferred to a user-controlled MULTI-DEVICE 2-of-3 before public; Emblem vault (no raw key) fine now. (2) push to GitHub for audit = DONE. (3) user-signed swaps = BUILT (UI: LIVE·Base mainnet badge + "Trade on Uniswap ↗" deep-link app.uniswap.org/swap?chain=base&inputCurrency=&outputCurrency= → user connects own wallet, signs own swap, trustless; don't reinvent Uniswap). (4) wire base-mainnet into app operator endpoints (mintCritical→OZ artifact + amm/create→uniswap.js) = "get ready", queued.
- RECOMMENDATION to user: pause #4 (more mainnet code) until the audit runs on this stable snapshot, so the audit isn't chasing a moving target; market already live so #4 is convenience not blocker.

## StampySwap — AUDIT #1 response (github PR #1, commit 2345961) — P0s + key P1s FIXED 2026-09-21
External audit (branch audit/2345961-custody-gates, PR #1) — high quality, correct. I did NOT blind-apply their 125KB patch; implemented equivalent fixes DIRECTLY on main (I know the code; safer to verify each). Verified each finding against real code first.
P0 (all FIXED + verified via curl):
- /api/custody/redeem (P0-1 vault DRAIN): released vault on SQL circulating counter alone, no burn proof. FIX: non-operator MUST pass burn_txid → verifyRepBurn(chain,txid,rep.dest_address,amt) (reuses /api/move's verifier) + dup-check burn_txid + reserve a PENDING collateral_ledger row BEFORE broadcast (crash-safe: retry sees pending→blocked; release-fail→marked 'failed') → then release → finalize (decrement + status='released'). operator header = back-office bypass. Added burn_txid column. redeemedBase now excludes status='failed'.
- /api/redeem (P0-2): public accounting-only burn desyncing PoR → 403 unless PREVIEW or operator.
- /api/amm/swap (P0-3): unauthenticated protocol-inventory mint → requireOperator. (Users trade via Uniswap deep-link.)
P1 (FIXED): verify-deposit getAssetByTick→resolveAsset (ACME/SOAP collision); uniswap swap minOut=quote−50bps + addLiquidity mins=99% (were 0); ACME_CONFIRMS default 1→3; wrap/route preview now = !hasBaseMainnetRep (was always testnet); XCP verify-deposit FROZEN operator-only (balance-delta flaw pending per-tx rewrite).
VERIFIED: PoR all_solvent unchanged; /api/redeem 403, /api/amm/swap 403, /api/custody/redeem 403-gated, wrap/route TREES preview:false. Committed 5222498. CUSTODY STILL GATED OFF (live:false) until residuals cleared.
RESIDUAL (still open, before mainnet-safe / public): (1) SRC-20 redeem decode PSBT payload before signPsbt (echo-asserts only now); (2) /api/move bind burn→signed receive addr + N confs (front-run class); (3) stamp-bridge pending-row-before-broadcast (redeem done, bridge open); (4) XCP true per-tx attribution rewrite (frozen for now); (5) trust panel copy refresh; (6) key-gen-on-first-import footgun (fail loud if key file missing); (7) redeemAcme independent review (888 fee / v1→v2 envelope); (8) ops: keep behind proxy, Emblem key=full vault, multisig not default; (9) soljson 9MB in git. Fixes NOT yet pushed (need STAMPYSWAP_TOKEN).

## StampySwap — AUDIT #1 residuals batch 2 (2026-09-21)
- #6 KEY-GEN FOOTGUN fixed: evm-signer/sol-mint/squads/safe now THROW if key file missing (unless STAMPY_ALLOW_KEYGEN=1) — no more silent key rotation → stranded funds. App still boots (keys exist). Fresh deploys must restore keys OR set the flag.
- #2 /api/move operator-gated (was public mint + unbound burn = front-run/gas-drain). Real user cross-chain move = future bound+signed feature. Committed 69288a8.
POSTURE NOW: every LIVE value-moving path is gated — /api/mint, /api/amm/create, /api/amm/swap, /api/move, /api/redeem(public), XCP verify-deposit = operator-only; /api/custody/redeem = gated + burn-proof; custody release = live:false. Reads + user-signed Uniswap trading stay public. PoR all_solvent.
REMAINING residuals (lower severity or frozen-so-safe): #1 SRC-20 redeem PSBT payload decode; #3 stamp-bridge pending-row (gated/inactive); #4 XCP per-tx attribution rewrite (frozen operator-only for now); #5 trust-panel copy refresh; #7 redeemAcme independent review (external); #8 ops (behind-proxy, Emblem-key=full-vault, multisig-not-default); #9 soljson 9MB git bloat. Fixes NOT pushed (need token). User has an OpenAI repair plan incoming — may overlap; consider pausing deep rewrites (#1,#4) until it lands to avoid churn.

## StampySwap — AUDIT #2 (OpenAI/Codex, 10 findings F01-F10) — PR1 CONTAINMENT done + pushed 2026-09-21
Second, deeper audit (same commit 2345961) — 10 findings, 6-PR repair plan. CONFIRMS my P0 fixes (F01 custody-redeem=my P0-1, F02 legacy redeem=P0-2, F05 move=my move-gate) + adds NEW: F03 (public /api/bridge/intent/:id/txid edit → replay/double-mint), F04 (XCP balance-delta), F06/F07 (crash recovery = mint/move idempotency), F08 (base-mainnet advertised but verify-deposit→mintCritical unsupported → records claim then fails), F09 (fractional ACME mints >deposited), F10 (stamp partial claim consumes whole burn).
DID PR1 (containment) — THE priority. Single maintenance middleware after express.json, BEFORE any DB write/signer: STAMPY_MAINTENANCE default ON. CONTAINED_PATTERNS block (503) all value/accounting routes for non-operators: redeem, custody/redeem, verify-deposit, mint, mint/migrate-authority, move, amm/{create,swap,deploy-mock}, preview/confirm-deposit, stampbridge/execute, AND bridge/intent(/*) [F03, incl the GET poll]. relayerSweep disabled in maintenance. Operator header bypasses (back-office). GET /api/status + UI sticky maintenance banner + JS disables value-action buttons. VERIFIED: all reads 200, all contained routes 503, status=maintenance. Neutralizes F01-F10 at the server. Committed b1c49ce, PUSHED 2345961..b1c49ce (remote clean, verified). PoR unaffected; deployed Uniswap pool trades independently.
IMPORTANT: the plan assumed my local = stale 77dea9b — WRONG. My main = audited 2345961 + my P0 fixes (5222498) + residuals (69288a8) + PR1 (b1c49ce). Built PR1 ON my main (did NOT reset to 2345961). Reused the user's most-recent PAT for the push.
REMAINING (repair-plan PRs 2-6, deep rewrites on the safe baseline, no rush now everything's contained): PR2 deposit attribution (per-tx source events, immutable, exact-amount, bind auth, ACME integer units F09) ; PR3 authorized burns for release/move (burn returns owner, shared consumption registry, stamp full-entitlement F10) ; PR4 durable op state machine (verified/reserved/submitted/confirmed, crash-safe F06/F07, UTXO serialization) ; PR5 chain integration (base-mainnet capability wiring F08, ACME source decimals, token-order reserves, pin OZ dep) ; PR6 reconcile state + real PoR + independent signers + CI. DO NOT reopen custody until these + independent review.

## StampySwap — AUDIT #2 concrete fixes batch (F09/F08/ACME-dec) 2026-09-21
On the contained baseline, knocked out self-contained correctness fixes:
- F09 (fractional over-mint): verify-deposit now rejects amounts with MORE precision than source decimals (toBaseUnits(floorToDecimals(amt,srcDec))!==toBaseUnits(amt)). Unit-tested: 1.9@0dp→REJECT, 1.0@0dp→ok, 1.123456789@8dp→REJECT. Closes "1 indivisible unit → 1.9 minted".
- F08 (advertise-but-cant-mint): verify-deposit fail-CLOSED unless chain===solana||evmMint.isSupported(chain) — no recording a claim mintCritical would reject (base-mainnet not yet in mint path).
- ACME redeem decimals: custody.redeemAcme+redeem take dec (was hardcoded 8); server passes redeemDec + acme qtyBase so the composed-quantity assertion RUNS for ACME (was skipped).
Pushed b1c49ce..6a8bb69. App boots, PoR solvent, containment intact.
STILL TODO (deep rewrites, PRs 2-4, on contained baseline — no rush): PR2 XCP per-tx attribution (replace balance-delta F04) + immutable source-event registry; PR3 burn-returns-owner authorization + shared consume-once registry across redeem/move (F05 full) + stamp full-entitlement (F10); PR4 durable op state machine (verified/reserved/submitted/confirmed) for mint+move crash-safety (F06/F07) + BTC UTXO serialization; PR5 wire base-mainnet mint (OZ artifact) + token-order reserves + pin OZ dep; PR6 reconcile + real PoR + independent signers + CI. Custody stays closed until these + independent re-review.

## StampySwap — AUDIT #2 PR3 (authorized burns) DONE 2026-09-21
- verifyBurn (evm-mint + sol-mint) now return the real BURNER (EVM Transfer.from topics[1]; Solana burn authority). server verifyRepBurn passes owner through.
- SHARED consume-once registry: consumed_burns table (burn_txid PRIMARY KEY) + consumeBurn/burnConsumed/normTxid(EVM lowercase, Solana as-is) helpers. VERIFIED: same burn_txid 2nd insert → UNIQUE fail → one burn = one action across redeem+move (F05/F10 class).
- /api/custody/redeem: non-operator now MUST (a) not-already-consumed, (b) verifyRepBurn, (c) sign an owner→BTC-recipient binding (verifyBurnOwnerSig: EVM ethers.verifyMessage / Solana nacl ed25519 over redeemBindingMsg), (d) consumeBurn reserve BEFORE release; on THROW (no txid) → free reservation (retryable) else fail-closed. VERIFIED owner-sig accept/reject (valid true, tampered false).
- /api/move: shared consumeBurn (records owner) replaces local dup-check.
Pushed 6a8bb69..621499e. App boots, PoR solvent, containment intact (move 503).
REMAINING: F10 stamp full-entitlement (bridge inactive, low pri); PR2 XCP per-tx attribution (frozen operator-only now); PR4 durable op state machine (F06/F07 mint/move crash-safety + BTC UTXO serialization — the big one); PR5 base-mainnet mint wiring + token-order reserves + pin OZ dep; PR6 reconcile + real PoR + independent signers + CI. Custody stays closed until PR2/PR4 + independent re-review.

## StampySwap — RE-AUDIT of 621499e (OpenAI/Codex, via WeTransfer) — R01/R02/R03 fixed 2026-09-21
Re-audit CONFIRMED my fixes landed (26 local tests): F01 core no-burn rejected, owner-sig check, F09 (both cases), F08 claim-loss guard, shared registry (redeem burn can't move), signature binding (tampered dest rejected), F02 live-mode, key-gen, slippage. BUT found a CRITICAL regression + open recovery defects.
FIXED THIS BATCH (pushed 621499e..1397e6e):
- R01 (CRITICAL): my maintenance regex-blacklist matched the RAW path, but Express 4 dispatches case-insensitive + optional trailing/double slashes → /API/.../ and /path/ BYPASSED containment → re-exposed F03 double-credit. FIX: DEFAULT-DENY (block ALL non-GET /api during maintenance — no list to enumerate/bypass) + normPath() (lowercase, strip trailing/dup slashes) used by BOTH maintenance + rate-limit. PERMANENTLY RETIRED (410, all cases): /api/redeem, /api/preview/confirm-deposit, /api/bridge/intent*, and killed the sweep. VERIFIED: canonical+trailing+uppercase+double-slash ALL 503; retired all 410; reads 200.
- R02: redeem catch was freeing the burn reservation on ANY throw — but a post-broadcast connection error is UNKNOWN, not proof-nothing-sent. FIX: only GATED (proven pre-send) frees + retries; any other error keeps burn consumed + status='reconcile' (fail-closed, no double-release).
- R03: added startup migrateSchema() — verifies burn-registry schema (SCHEMA_OK; if missing, containment holds even with MAINTENANCE off) + idempotent backfill of historical move-out/redeem burns into consumed_burns (per-chain case-norm). App can't DDL (Dashboard API blocks CREATE/ALTER) → schema applied out-of-band by agent; documented.
STILL OPEN (deep, contained-safe): R04 partial-entitlement/exact-claim (+F10 stamp); R05 durable op state machine (F06/F07 crash recovery — THE big one, no delete-on-error, cross-process, UTXO serialize); R06 per-chain burn FINALITY (EVM verifier accepts 1-block burn → reorg risk); R07 truthful capability/status model (wrap enabled:true + deposits_live:true during maintenance; UI redeem form doesn't send burn_txid/auth_sig); F04 XCP per-send attribution (operator-restricted meanwhile); F05 move holder-auth (moveBindingMsg unused; operator-only meanwhile). Plus: canonical-ID identity everywhere, PSBT/envelope content validation, pin OZ dep, CI/tests, reconciliation. Custody stays CLOSED.

## StampySwap — RE-AUDIT 621499e: R06 + R07 fixed 2026-09-21
- R06 burn FINALITY: solMint.verifyBurn now queries commitment='finalized'; evmMint.verifyBurn returns confirmation depth; verifyRepBurn enforces per-chain min (BURN_FINALITY base 3 / ethereum 12 / base-mainnet 20, env BASE_MAINNET_BURN_CONFIRMATIONS) + now routes base-mainnet burns to the EVM verifier (chain in DEST_DECIMALS, not the CHAINS check). Shallow burn → valid:false 'not final'.
- R07 truthful status: wrap/route enabled=wl && !MAINTENANCE + service + honest reason; /api/custody/status deposits_live=false + service=maintenance + note when paused. VERIFIED both. (UI redeem-form still doesn't send burn_txid/auth_sig/chain — part of reopening client work, custody closed so moot now.)
Pushed 1397e6e..d42f9fb.
BIG REMAINING = R05 (durable operation state machine, F06/F07): persist op identity + states (verified/reserved/submitted/confirmed/definite-presend-fail/uncertain), atomic DB reservation, cross-PROCESS coordination (in-mem locks insufficient), persist signed-txid before broadcast + reconcile, never delete reservation to force retry, serialize shared BTC vault UTXOs across assets, apply to deposit/mint/move/redeem/stamp. THIS is the reopening gate — large architectural build, do as its own effort. Also remaining: R04 exact/partial entitlement + F10 stamp; F04 XCP per-send attribution; F05 move holder-auth (moveBindingMsg still unused — wire it when move reopens to users); canonical-ID identity everywhere; PSBT/envelope content validation; pin OZ dep + CI/tests; historical reconciliation. Custody CLOSED until R05 + independent re-review.
SESSION: 3 audit rounds handled. Fixed F01/F02/F03(retired)/F05(redeem)/F08/F09 + R01/R02/R03/R06/R07 + P0s + containment + key-footgun + slippage + XCP/move freeze. Every push verified no-secrets. main HEAD d42f9fb.

## StampySwap — R05 part 1 (durable ops, mint crash-safety F06/F07) 2026-09-21
Built the operations table (op_key PK = cross-process idempotency via shared DB) + opReserve/opComplete/opFail/opReconcile. mintCritical(opKey): reserve before on-chain mint → opComplete(txid) BEFORE accounting writes (so a failed DB update can't re-mint on retry = F07 fixed) → idempotent resume on completed → UNFUNDED/BADADDR=opFail(retryable), other=opReconcile(never re-mint uncertain). deposit-mint + move-mint pass deterministic op_keys. verify-deposit F06: revert the collateral credit on definite pre-send mint failure (retryable) / keep on uncertain (reconcile). Added operations to migrateSchema check. Boots, solvent, containment intact. Pushed d42f9fb..07672db.
REMAINING R05: move F06 ROLLBACK (undo source decrement + burn on pre-send dest-mint fail); op-guard the RELEASE/redeem + stamp paths (currently R02-level reconcile only); serialize shared BTC vault UTXOs across assets; full fault-injection tests. Plus R04 exact/partial entitlement, F04 XCP per-send attribution, F05 move holder-auth (wire moveBindingMsg), UI redeem burn+sign flow, canonical-ID everywhere, PSBT content validation, pin OZ dep, CI. Custody CLOSED.
STRONG RECOMMENDATION: re-run the OpenAI/Codex harness vs main HEAD 07672db now — it holds R01-R07 + R05p1, a large batch not yet harness-verified. Verify these land (like the round that caught my R01 bypass) BEFORE more R05 building.
