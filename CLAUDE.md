
## StampySwap — ACME protocol adapter (Phase 0 discovery) integrated at peer "acme"'s request (2026-08-21)
Peer agent (group "acme") dropped acme.js + test-acme.js in the dir, claimed head-dev approval (UNVERIFIED — flagged to user). REVIEWED both first: acme.js = pure read-only HTTP to acme.pics (env ACME_API), no fs/keys/eval/writes, no top-level side effects, mirrors counterparty.js (copied my fromBase/toBase incl leading-zero fix); composeSend (redeem-only, gated upstream) has >2^53 guard. test-acme.js = read-only GETs + 1 dry composeSend (fails "insufficient funds", nothing broadcast). Ran test → all green (ACME/SOAP lookup, holders-based balance 70M correct, attribution, chainHeight 963429, composeSend expected-error).
WIRED (additive, reversible): const acme=guarded-require; discoverAcme(raw,cands,res) mirrors discoverCounterparty, source_protocol='acme', deploy_tx=acme:{asset}, whitelisted=0 (NOT auto-whitelisted → INERT: an acme asset can't flow through SRC-20/XCP deposit/mint paths, zero value risk). Dispatch: src-20 → counterparty → acme (acme only fires when both miss). Did NOT touch custody/mint/AMM.
VERIFIED: HERESY.ED → source_protocol='acme' row id20 (decimals 0, max_supply 100, deploy_tx acme:HERESY.ED, locked). Regression: KEVIN=src-20, PUDSEC=counterparty still work.
FINDING reported to acme: TICKER COLLISION across protocols — SOAP/ACME/SADIE also exist as SRC-20/Counterparty so those win (dispatch precedence); only ACME-unique names (.ED/.#### subassets) resolve as acme. Bare-name search can't reach a shadowed ACME asset. Options: protocol-prefixed search, multi-protocol disambiguation, or accept precedence.

## StampySwap — ACME Phase 1 (custody DETECTION) built — head-dev DIRECTLY authorized (2026-08-27)
Head dev (kevmart) confirmed IN OUR CHANNEL: "I authorize Phase 1: ACME custody detection (gated, tiny test)." (The peer's relayed approval was NOT enough — I'd required + waited for first-party confirmation; correct gate held.) PHASE1-PREP.md reviewed: attribution SOLVED (findDeposits per-tx), redemption = envelope NOT PSBT (heavier Phase-4, doesn't block detection), finality = confirmed-only + N-conf gate. acme.js Phase-1 helpers (findDeposits, confirmations) reviewed clean/read-only; existing fns unchanged.
BUILT (custody-side, but DETECTION-only, nothing releasable):
- ACME_CONFIRMS (env ACME_CONFIRMATIONS, default 1) finality gate.
- verify-deposit ACME branch: ACME has per-tx attribution so mirrors the SRC-20 TXID path (cleaner than XCP balance-delta) — findDeposits match by tx_hash + source-binding (dep.source==boundSource) + confirmations()>=ACME_CONFIRMS + ledgerKey=`acme:${txid}` (unique-index idempotent). Mints only if asset whitelisted (test asset NOT whitelisted → inert).
- NEW read-only GET /api/custody/detect?tick=&source=&amount= — findDeposits + confirmations report {detected, deposits:[{tx_hash,source,quantity,block,confirmations,confirmed}], vault_balance}. NOTHING credited/minted/released. This is the tiny-test surface.
PROVEN: findDeposits('ACME',known-DST,{source})→5 real matches w/ confs 1216/5280 valid; /api/custody/detect?tick=HERESY.ED vs our vault → detected:0 vault_balance:0 (honest empty, read-only). Collision note: getAssetByTick can't disambiguate collided tickers → tiny test MUST use an ACME-UNIQUE ticker (HERESY.ED etc.).
TINY TEST plan: head dev/acme send tiny ACME-unique asset → vault bc1qtd5kg3akr3tme6578a03gm78dfzfg6fahne74v → run /api/custody/detect → prove detection. Nothing mints (detection-only; verify-deposit mint stays gated behind whitelist=0). ACME redemption (Phase 4) = envelope→PSBT construction, NOT built.
